The script checks whether there's enough disk space available on the root directory. 

If the disk space is greater than or equal to the given input, the script prints "WARNING Disk usage is at ${disk_usage}% - threshold exceeded" to the terminal.

If the disk space is less than the given input, the script prints "OK: Disk usage is at ${disk_usage}% - within normal range"

