The script checks whether at least one argument is provided, and if not displays an error message.

For each file/arguemnt provided: the script checks the file exists and is readable and counts/displays the number of lines containing the word "error".

If the file doesn't exist or isn't readable, the script displays "File $FILE - Not accessible"

The script accepts multiple arguments.
